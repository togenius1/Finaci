export class Cash {
  constructor(id, title) {
    this.id = id;
    this.title = title;
  }
}

export class Account {
  constructor(id, title) {
    this.id = id;
    this.title = title;
  }
}

export class Credit {
  constructor(id, title) {
    this.id = id;
    this.title = title;
  }
}
