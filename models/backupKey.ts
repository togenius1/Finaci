export default class BackupKey {
  constructor(id, key, key) {
    this.id = id;
    this.key = key;
  }
}

// export default Expense;

export interface BackupKeyType {
  id: string;
  title: string;
}
