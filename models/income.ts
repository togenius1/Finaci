class Income {
  constructor(id, cateId, accountId, amount, note, date) {
    this.id = id;
    this.cateId = cateId;
    this.accountId = accountId;
    this.amount = amount;
    this.note = note;
    this.date = date;
  }
}

export default Income;
